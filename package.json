{
  "name": "ter",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "dev": "node index.js",
    "test": "echo \"Error: no test specified\" ",
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/run.js --format=cjs",
    "clean": "rm -rf dist"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "type": "module"
}
